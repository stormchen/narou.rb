:javascript
  var busy = $("#rebooting-busy");
  busy.text(".");
  setInterval(function() {
    busy.text(busy.text() + ".");
    if (busy.text().length == 6) {
      busy.text(".");
    }
  }, 500);

%p
  伺服器正在重新啟動<span id="rebooting-busy"></span>
%p
  重啟完成後將會自動重新載入頁面。
%p
  若長時間未重新載入，
  %a(href="/") 請點擊此處
  或按下瀏覽器的重新整理按鈕。
